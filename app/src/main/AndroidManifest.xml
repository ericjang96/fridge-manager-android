<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kevin.fridgemanager">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".Activities.FridgeActivity"
            android:launchMode = "singleInstance"
            android:parentActivityName=".Activities.MainActivity">
        </activity>

        <activity
            android:name=".Activities.UserGroceryListsActivity"
            android:launchMode = "singleInstance"
            android:parentActivityName=".Activities.MainActivity">
        </activity>

        <activity
            android:name=".Activities.LoginActivity"
            android:launchMode = "singleInstance"
            android:parentActivityName=".Activities.MainActivity">
        </activity>

        <activity
            android:name=".Activities.SignupActivity"
            android:launchMode = "singleInstance"
            android:parentActivityName=".Activities.LoginActivity">
        </activity>


        <activity
            android:name=".Activities.GroceryListActivity"
            android:launchMode = "singleInstance"
            android:parentActivityName=".Activities.UserGroceryListsActivity">
        </activity>
    </application>

</manifest>